//#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdio.h>


/* FUNCTION PROTOTYPES */
size_t *preprocess(const char *pattern);
size_t kmpSearch(const char *text, const char *pattern);
